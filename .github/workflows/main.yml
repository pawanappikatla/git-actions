name: testing cicd pipeline

on:
  push:
    branches: [ "master" ]
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: List files
      run: ls -ltr
    - name: Echo simple message
      run: echo "Hello Pawan"
  
